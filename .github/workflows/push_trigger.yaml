name: Publish mosip-helm charts

on:
  push:
    branches:
      - 1.2.0.*
      - develop
    paths:
      - 'charts/**'

jobs:
  Generate_Documentation:
    uses: syedsalman3753/kattu/.github/workflows/publish-helm-charts.yml@master
    with:
      HELM_REPO_ACCOUNT: syedsalman3753